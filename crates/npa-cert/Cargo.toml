[package]
name = "npa-cert"
version = "0.3.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
npa-kernel = { path = "../npa-kernel" }
sha2 = "0.10"

[dev-dependencies]
libc = "0.2"
