[package]
name = "npa-checker-ref"
version = "0.3.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
sha2 = "0.10"

[dev-dependencies]
libc = "0.2"
npa-cert = { path = "../npa-cert" }
npa-kernel = { path = "../npa-kernel" }
