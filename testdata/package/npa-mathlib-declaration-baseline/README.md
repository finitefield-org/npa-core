# Declaration Promotion Baseline

This empty `npa-mathlib` snapshot is the clean target baseline for focused
declaration-promotion integration tests. The promotion fixture creates its
first module and migrates the empty registry from v1 to v2.
